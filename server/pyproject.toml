[project]
name = "buzz-server"
version = "0.5.0"
description = "Self-hosted static site hosting server"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-multipart>=0.0.12",
    "jinja2>=3.1.0",
    "dnspython>=2.8.0",
    "idna>=3.11",
    "webauthn>=3.0.0",
    "httpx-oauth>=0.17",
]

[project.scripts]
buzz-server = "server.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/server"]
sources = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "httpx>=0.27.0",
    "soft-webauthn>=0.1.3",
]
